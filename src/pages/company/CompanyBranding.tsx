import { PagePlaceholder } from "@/components/PagePlaceholder";
import { Palette } from "lucide-react";

export default function CompanyBranding() {
  return <PagePlaceholder title="Marca e Personalização" description="Configure logo, cores, textos institucionais e identidade visual das apresentações." icon={Palette} />;
}
