import { PagePlaceholder } from "@/components/PagePlaceholder";
import { Settings } from "lucide-react";

export default function AdminSettings() {
  return <PagePlaceholder title="Configurações Globais" description="Configurações gerais da plataforma, planos e integrações." icon={Settings} />;
}
