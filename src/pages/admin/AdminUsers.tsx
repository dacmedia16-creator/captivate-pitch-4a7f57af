import { PagePlaceholder } from "@/components/PagePlaceholder";
import { Users } from "lucide-react";

export default function AdminUsers() {
  return <PagePlaceholder title="Usuários" description="Gerencie todos os usuários do sistema em todas as imobiliárias." icon={Users} />;
}
