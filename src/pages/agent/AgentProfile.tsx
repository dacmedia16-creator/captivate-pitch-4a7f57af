import { PagePlaceholder } from "@/components/PagePlaceholder";
import { UserCircle } from "lucide-react";

export default function AgentProfile() {
  return <PagePlaceholder title="Meu Perfil" description="Configure sua foto, bio, conquistas e informações profissionais para as apresentações." icon={UserCircle} />;
}
